package org.zalando.rxnakadi.domain;

/**
 * Bean Validation group that applies to domain classes that have beeen received from Nakadi.
 */
public interface Inbound {
    // Just a marker interface.
}
